# Proyecto

## Dashboard (React)

```bash
npm install
npm run dev
```

Abre la URL que muestra Vite (por defecto `http://localhost:5173`) para ver el dashboard.

Los archivos `Login.html` y `Register.html` siguen siendo estáticos en la raíz del repo.